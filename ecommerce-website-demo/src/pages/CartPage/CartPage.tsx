import CartSection from './CartSection';

const CartPage = () => {
      return (
            <main className='max-w-[1170px] mx-auto'>
                  <CartSection/>
            </main>
      );
};

export default CartPage;