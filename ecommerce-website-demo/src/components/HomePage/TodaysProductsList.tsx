import React from 'react';

const TodaysProductsList = () => {
      return (
            <section>
                  todays product list
            </section>
      );
};

export default TodaysProductsList;